import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#00DFA2",
      contrastText: "#fff",
    },
    secondary: {
      main: "#8062D6",
    },
    success: {
      main: "#00DFA2",
    },
  },
});
